import { UniqueUsernameOrEmailValidator } from './unique-username-or-email-validator';

describe('UniqueUsernameOrEmailValidator', () => {
  it('should create an instance', () => {
    expect(new UniqueUsernameOrEmailValidator()).toBeTruthy();
  });
});
